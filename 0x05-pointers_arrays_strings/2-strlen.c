#include "main.h"
#include <string.h>

/**
*_strlen - function for counting letters
*@s : a char pointer
*
*Return: return integer number of letters
*/

int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
