#include "main.h"
#include <string.h>

/**
*_strcat - function of pointers
*@dest:
*@src:
*
*Return: return p which is
*/

char *_strcat(char *dest, char *src)
{
	char *p;
	p = strcat(dest,src);
	return (p);
}
